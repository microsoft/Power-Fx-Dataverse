Concat(__retrieveMultiple(local, __lt(local, new_price, 120), __noop(), 999, ), (Concatenate(DecimalToText(new_price), ,)))
Concat(__retrieveMultiple(local, __noop(), __noop(), Float(2), ), (Concatenate(DecimalToText(new_price), ,)))
Concat(__retrieveMultiple(local, __noop(), __noop(), 999, , {new_price:new_price}), (Concatenate(DecimalToText(new_price), ,)))
Concat(local, (Concatenate(DecimalToText(new_price), ,)))
