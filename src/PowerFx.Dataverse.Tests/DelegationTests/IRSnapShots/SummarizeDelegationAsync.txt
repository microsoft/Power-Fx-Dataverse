__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount), ) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1000
__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount, Sum(credit) As T Credit), ) | $apply=groupby((name),aggregate(amount with sum as TotalAmount,credit with sum as T Credit))&$top=1000
__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, credit, Sum(amount) As TotalAmount), ) | $apply=groupby((name,credit),aggregate(amount with sum as TotalAmount))&$top=1000
__retrieveMultiple(t1, __eq(t1, {fieldFunctions:Table(), fieldName:name}, test), __noop(), 1000, __groupBy(name, credit, Sum(amount) As TotalAmount), ) | $apply=filter=(name eq 'test')/groupby((name,credit),aggregate(amount with sum as TotalAmount))&$top=1000
__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Count(amount) As TCount), ) | $apply=groupby((name),aggregate($count as TCount))&$top=1000
__retrieveMultiple(t1, __noop(), __noop(), Float(10), __groupBy(name, Sum(amount) As TotalAmount), ) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=10
__retrieveSingle(t1, __noop(), __noop(), __groupBy(name, Sum(amount) As TotalAmount), ) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1
ShowColumns(__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount), ), TotalAmount) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1000
Summarize(ShowColumns(t1, name, amount), name, ({TCount:CountIf(ThisGroup, (Not(IsBlank(amount))))}))
Summarize(ForAll(t1, ({amount:amount, Name:name})), Name, ({TCount:CountIf(ThisGroup, (Not(IsBlank(amount))))}))
Summarize(Summarize(t1, name, ({TotalAmount:Sum(ThisGroup, (amount))})), name)
Summarize(t1, name, ({TotalAmount:Sum(ThisGroup, (MulNumbers(amount,Float(2))))}))
Summarize(t1, name, credit, ({TotalAmount:Sum(ThisGroup, (Sqrt(Coalesce(amount, 0))))}))
FirstN(SortByColumns(Summarize(ForAll(Filter(t1, (EqText(name,test))), ({amount:amount, Name:name})), Name, ({COUNT_Id:CountIf(ThisGroup, (Not(IsBlank(amount))))})), COUNT_Id, (SortOrder).Descending), Float(5))
Sort(__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount), ), (TotalAmount)) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1000
SortByColumns(__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount), ), name) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1000
FirstN(SortByColumns(Summarize(ForAll(t1, ({amount:amount, Name:name})), Name, ({COUNT_Id:CountIf(ThisGroup, (Not(IsBlank(amount))))})), COUNT_Id, (SortOrder).Descending), Float(5))
Filter(__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount), ), (GtNumbers(TotalAmount,Float(10)))) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1000
LookUp(__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount), ), (GtNumbers(TotalAmount,Float(10)))) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1000
Distinct(__retrieveMultiple(t1, __noop(), __noop(), 1000, __groupBy(name, Sum(amount) As TotalAmount), ), (TotalAmount)) | $apply=groupby((name),aggregate(amount with sum as TotalAmount))&$top=1000
